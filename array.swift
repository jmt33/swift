extension Array{
    //随机排序
    func join(s:String)->String{
        return s.join(self)
    }
    //是否在数组内
    func has(){
        
    }
    func random(){
        
    }
    /*
        reverse()
        removeAtIndex(4)
        removeLast()
        removeAll()
    */
}
var arr=["1","2","3"]

    arr.join("|")